extends ../base


block title
  | FOJI | Registro de Usuario

block body
  .wrapper(aria-page="registration_coordinator")
    include ../navbar
    .float-content.catastro-title.mt-5
      h4 Formulario
      h1 Catastro foji
      .title-separator
      p 
        | Este formulario permite a las orquestas y ensambles ser parte de la
        | base de datos de la Fundaci&oacute;n, para poder informarse de las
        | diversas actividades que realizamos y para acceder a todos los
        | beneficios que esta entrega.
    .container 
      .alert.alert-primary.mt-3.mb-3.text-center(role="alert") Ingresa tus datos a continuación para crear tu cuenta de coordinador de la orquesta.
    .full-box.box-form.container
      form#coordinator-form(method='POST' name='coordinator-form')
        | {% csrf_token %}
        .box-title
          h4 Registro Usuario Coordinador 
        .box-content.container
          .row
            .col-md-6.pt3.pb-3.p-sm-5
              .form-row
                .form-group.col-sm-6
                  label(for="coordinator-first_name") Nombre:
                  input#coordinator-first_name.form-control(
                    name="coordinator-first_name"
                    value='{{ personal_information_form.first_name.value }}'
                    type="text"
                    placeholder="Escribe tu nombre"
                    required
                  )
                  .invalidFeedback
                    | {{ personal_information_form.first_name.errors.0 }}
                .form-group.col-sm-6
                  label(for="coordinator-last_name") Apellido:
                  input#coordinator-last_name.form-control(
                    name="coordinator-last_name"
                    value='{{ personal_information_form.last_name.value }}'
                    type="text"
                    placeholder="Escribe tu apellido"
                    required
                  )
                  .invalidFeedback
                    | {{ personal_information_form.last_name.errors.0 }}
              .form-group
                label(for="coordinator-social_id") RUT:
                input#coordinator-social_id.form-control(
                  name="coordinator-social_id"
                  value="{{ personal_information_form.social_id.value }}"
                  type="text"
                  placeholder="Ej: 15.642.495-8"
                  required
                )
                .invalidFeedback#rutFeedback
                    | {{ personal_information_form.social_id.errors.0 }}
              .form-group
                label(for="coordinator-phone_number_mobile") Teléfono:
                input#coordinator-phone_number_mobile.form-control(
                  name="coordinator-phone_number_mobile"
                  value="{{ personal_information_form.phone_number_mobile.value }}"
                  type="text"
                  placeholder="Ej: 962129365"
                  required
                )
                .invalidFeedback
                    | {{ personal_information_form.phone_number_mobile.errors.0 }}
              .form-group
                label(for="role") Cargo:
                input#role.form-control(
                  name="role"
                  value="{{ coordinador_form.role.value }}"
                  type="text"
                  placeholder="Tu cargo en la orquesta"
                )
                .invalidFeedback
            .col-md-6.pt-3.pb-3.p-sm-5.border-left
              .form-group
                label(for="email") Email:
                input#email.form-control(
                  name="email"
                  value="{{ user_form.email.value }}"
                  type="email"
                  placeholder="Tu email"
                  required
                )
                .invalidFeedback
                    | {{ user_form.email.errors.0 }}
              .form-group
                label(for="password") Contraseña:
                input#password.form-control(name="password" type="password"
                placeholder="Escribe tu contraseña" required)
                .invalidFeedback
                    | {{ user_form.password.errors.0 }}
              .form-group
                label(for="password_repeat") Repetir contraseña:
                input#password_repeat.form-control(name="password_repeat"
                type="password" placeholder="Debe coincidir con el anterior"
                required)
                .invalidFeedback
                    | {{ user_form.password_repeat.errors.0 }}
              .form-group
                label Fecha de nacimiento:
                #datepicker-input
                .invalidFeedback
                  | {{ personal_information_form.birth_date.errors.0 }}
        .bottom-box.text-right
          button#submitBtn.btn.btn-primary(type='submit') Siguiente
